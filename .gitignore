# ── 依赖与构建产物 ──
node_modules/
dist/
*.tsbuildinfo

# 构建产物不入库：由 CI 在打 tag 时构建并随 Release 分发
# （后端启动时从 web/dist 读取，本地开发用 npm run build 自行生成）

# ── 本地环境 ──
.env
.env.local
.env.*.local

# ── 编辑器 / 工具 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
.claude/

# ── 日志 ──
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
